# Python 3.11 recommended (matches the reference environment).
# Note: mlflow is a moderately heavy install (pulls in many transitive deps)
# and can take a few minutes the first time.
Flask==3.1.0
Werkzeug==3.1.3
mlflow==2.21.3
PyPDF2==3.0.1
requests==2.32.3
